`turbo.js` is Turbo 8.0.23, the asset served by the local Rails application at
`/assets/turbo.min-9fd88cd5.js` when this navigation refactor was implemented.
Its MIT license is in `Turbo-LICENSE`. It is bundled only in the test target so
WKWebView integration tests can exercise real Turbo without Rails or network access.
